name: Apply YouTube local timestamp source

on:
  pull_request:
    branches: [main]

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke test
        run: |
          echo "runner-ok"
          node --version
          git --version
